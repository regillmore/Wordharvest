# Assets

Static assets in this directory are copied directly into the Vite build.

Recommended early structure:

- `sprites/`: sprite sheets, tiles, portraits, icons, and atlases.
- `audio/`: music, ambience, sound effects, and generated audio sprites.

Before adding polished or generated assets, check `docs/STYLE_GUIDE.md` for palette, resolution, tile size, outline rules, animation constraints, naming, and export settings.
